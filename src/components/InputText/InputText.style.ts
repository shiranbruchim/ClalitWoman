import {StyleSheet} from 'react-native';

export const InputTextStyle = StyleSheet.create({
  textInputStyle: {
    borderBottomWidth: 1,
    borderColor: '#c1e4f9',
    minWidth: '100%',
    fontSize: 18,
    textAlign: 'right',
    alignItems: 'flex-end',
  },
});
